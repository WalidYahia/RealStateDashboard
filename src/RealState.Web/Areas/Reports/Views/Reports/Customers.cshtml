@model CustomerReportVm
@{
    ViewData["Title"] = "تقرير العملاء";
    string N(decimal v) => v.ToString("N0", new System.Globalization.CultureInfo("ar-EG"));
    string? F(DateTime? d) => d?.ToString("yyyy-MM-dd", System.Globalization.CultureInfo.InvariantCulture);
}
<div class="page-head">
    <h1>تقرير العملاء</h1>
    <a class="btn-new-tenant" asp-action="CustomersPrint" asp-route-from="@F(Model.From)" asp-route-to="@F(Model.To)" target="_blank">🖨 طباعة (PDF)</a>
</div>

<div class="card-tile" style="margin-bottom:12px;">
    <form asp-action="Customers" method="get" style="display:flex;gap:10px;align-items:end;flex-wrap:wrap;">
        <div><label class="form-label">من (تاريخ العقد)</label><input type="date" name="from" value="@F(Model.From)" class="form-control" style="margin:0;" dir="ltr" /></div>
        <div><label class="form-label">إلى</label><input type="date" name="to" value="@F(Model.To)" class="form-control" style="margin:0;" dir="ltr" /></div>
        <button type="submit" class="btn-primary" style="width:auto;">بحث</button>
        <a class="btn-sm" asp-action="Customers">اليوم</a>
        <a class="btn-sm" asp-action="Customers" asp-route-from="@F(DateTime.Today.AddDays(-7))" asp-route-to="@F(DateTime.Today)">آخر أسبوع</a>
        <a class="btn-sm" asp-action="Customers" asp-route-from="@F(DateTime.Today.AddMonths(-1))" asp-route-to="@F(DateTime.Today)">آخر شهر</a>
    </form>
</div>

<div class="card-tile">
    <div class="list-search-bar"><input class="form-control list-search" data-target="#custRepTable" placeholder="🔍 بحث في القائمة..." /></div>
    <table class="rs-table" id="custRepTable" data-live-totals>
        <thead>
            <tr><th>العميل</th><th>الهاتف</th><th>عدد العقود</th><th>قيمة العقود</th><th>أقساط متبقية</th><th>المحصَّل</th><th>المتبقي</th></tr>
        </thead>
        <tbody>
        @foreach (var r in Model.Rows)
        {
            <tr>
                <td>@r.Name</td>
                <td dir="ltr" style="text-align:right;">@r.Phone</td>
                <td>@r.Contracts</td>
                <td>@N(r.ContractsValue)</td>
                <td>@r.RemainingInstallments</td>
                <td class="tag-up">@N(r.Collected)</td>
                <td>@N(r.Residual)</td>
            </tr>
        }
        @if (Model.Rows.Count == 0)
        {
            <tr data-skip-search><td colspan="7" style="color:var(--muted)">لا توجد بيانات في هذه الفترة.</td></tr>
        }
        <tr data-no-results data-skip-search style="display:none;"><td colspan="7" style="color:var(--muted)">لا توجد نتائج مطابقة للبحث.</td></tr>
        </tbody>
        <tfoot>
            <tr>
                <th colspan="2">الإجمالي</th>
                <th>@Model.TotContracts</th>
                <th>@N(Model.TotValue)</th>
                <th>@Model.TotRemInst</th>
                <th>@N(Model.TotCollected)</th>
                <th>@N(Model.TotResidual)</th>
            </tr>
        </tfoot>
    </table>
</div>
