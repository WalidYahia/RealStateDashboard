@model List<UnitOfMeasure>
@{
    ViewData["Title"] = "وحدات القياس";
    var canManage = User.Can(PermissionNames.InventoryManage);
}
<div class="page-head">
    <h1>وحدات القياس</h1>
    <span style="display:flex;gap:8px;">
        <a class="btn-sm" asp-action="Print" target="_blank">🖨 طباعة (PDF)</a>
        <a class="btn-sm" asp-action="Excel">⬇ Excel</a>
        @if (canManage)
        {
            <button type="button" class="btn-new-tenant" onclick="appOpenModal('@Url.Action("Form")', 'وحدة قياس جديدة')">➕ وحدة جديدة</button>
        }
    </span>
</div>

@if (TempData["StatusMessage"] is string status) { <div class="status-message">@status</div> }
@if (TempData["ErrorMessage"] is string err) { <div class="status-message" style="background:rgba(208,59,59,.12);border-color:var(--critical);color:var(--critical);">@err</div> }

<div class="card-tile">
    <table class="rs-table">
        <thead><tr><th>الكود</th><th>الاسم</th><th>الحالة</th><th></th></tr></thead>
        <tbody>
        @foreach (var u in Model)
        {
            <tr>
                <td dir="ltr" style="text-align:right;">@u.Code</td>
                <td>@u.Name</td>
                <td>@(u.IsActive ? "مفعّل" : "متوقف")</td>
                <td style="white-space:nowrap;">
                    @if (canManage)
                    {
                        <button type="button" class="btn-sm" onclick="appOpenModal('@Url.Action("Form", new { id = u.Id })', 'تعديل الوحدة')">✏️ تعديل</button>
                        <form asp-action="Delete" asp-route-id="@u.Id" method="post" style="display:inline;" data-confirm="حذف الوحدة «@u.Name»؟" data-danger>
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn-sm btn-danger">حذف</button>
                        </form>
                    }
                </td>
            </tr>
        }
        @if (Model.Count == 0) { <tr><td colspan="4" style="color:var(--muted)">لا توجد وحدات بعد.</td></tr> }
        </tbody>
    </table>
</div>
