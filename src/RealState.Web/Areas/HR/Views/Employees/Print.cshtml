@model List<EmployeeListItem>
@{
    Layout = null;
    var tenantId = (Guid)ViewBag.TenantId;
    var withSalary = ViewBag.WithSalary as bool? ?? false;
    var withNationalId = ViewBag.WithNationalId as bool? ?? false;
    string N(decimal v) => v.ToString("N0", new System.Globalization.CultureInfo("ar-EG"));
    var totalSalary = Model.Sum(e => e.BasicSalary);
    var baseCols = 6 + (withNationalId ? 1 : 0);   // # + الاسم + الهاتف + القسم + الوظيفة + نوع التعاقد (+ الرقم القومي)
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <title>قائمة الموظفين</title>
    <style>
        body { font-family: "Segoe UI", Tahoma, sans-serif; padding: 24px; color: #111; }
        .brand { text-align: center; margin-bottom: 6px; } .brand img { max-height: 60px; }
        h1 { font-size: 19px; text-align: center; margin: 4px 0; }
        .sub { text-align: center; color: #666; font-size: 12px; margin-bottom: 14px; }
        table { width: 100%; border-collapse: collapse; font-size: 12px; }
        th, td { border: 1px solid #ccc; padding: 5px 7px; text-align: start; unicode-bidi: plaintext; }
        th { background: #f2f2f2; }
        tfoot td, tfoot th { font-weight: 700; background: #fafafa; }
        .no-print { margin-bottom: 12px; } @@media print { .no-print { display: none; } }
    </style>
</head>
<body>
    @await Component.InvokeAsync("ReportBackground", new { part = "open" })
    <div class="no-print"><button onclick="window.print()">🖨 طباعة / حفظ PDF</button></div>
    <h1>قائمة الموظفين</h1>
    <div class="sub">عدد الموظفين: @Model.Count · طُبع @DateTime.Now.ToString("yyyy/MM/dd HH:mm")</div>

    <table>
        <thead>
            <tr>
                <th>#</th><th>الاسم</th><th>الهاتف</th><th>القسم</th><th>الوظيفة</th><th>نوع التعاقد</th>
                @if (withNationalId) { <th>الرقم القومي</th> }
                @if (withSalary) { <th>الراتب الأساسي</th> }
            </tr>
        </thead>
        <tbody>
        @{ var i = 1; }
        @foreach (var e in Model)
        {
            <tr>
                <td>@i</td>
                <td>@e.Name</td>
                <td>@e.Phone</td>
                <td>@e.Department</td>
                <td>@e.Role</td>
                <td>@e.EmploymentType</td>
                @if (withNationalId) { <td dir="ltr" style="text-align:right;">@e.NationalId</td> }
                @if (withSalary) { <td>@N(e.BasicSalary)</td> }
            </tr>
            i++;
        }
        @if (Model.Count == 0) { <tr><td colspan="@(baseCols + (withSalary ? 1 : 0))">لا يوجد موظفون.</td></tr> }
        </tbody>
        @if (withSalary)
        {
            <tfoot>
                <tr><th colspan="@baseCols">إجمالي الرواتب الأساسية</th><th>@N(totalSalary)</th></tr>
            </tfoot>
        }
    </table>

    <script>window.addEventListener('load', function () { setTimeout(function () { window.print(); }, 400); });</script>
    @await Component.InvokeAsync("ReportBackground", new { part = "close" })
</body>
</html>
